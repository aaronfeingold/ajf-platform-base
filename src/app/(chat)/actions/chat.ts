// TODO: create axios requests to chat with an LLM

// createConversation() -> returns a new Convo Obj
// editConversation(title) -> returns an updated Convo Obj
// deleteConversation() -> returns a success message
// fetchConversations() -> returns a list of Convo Objs
// fetchConversationById(convoId) -> returns a single Convo Obj
// postMessage(convoId, message) -> sends a new message for returns a new LLM Response; type should have a convoID and messageId
// stopResponseGeneration(messageId) -> stops the LLM from generating a response to the last message
