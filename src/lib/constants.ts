const API_VERSION = "v0";
const DB_NAME = "propertyDB";
const DB_VERSION = 1;
const STORE_NAME = "propertyStore";
const DATA_KEY = "propertyData";
const SIX_MONTHS_MS = 6 * 30 * 24 * 60 * 60 * 1000; // Approximately 6 months in milliseconds

export {
  API_VERSION,
  DB_NAME,
  DB_VERSION,
  STORE_NAME,
  DATA_KEY,
  SIX_MONTHS_MS,
};
